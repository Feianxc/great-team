interface:
  display_name: "Great Team"
  short_description: "Borrow a great team before you build"
  default_prompt: "Use $great-team to assemble a great team around this idea: what is the wedge, what should we cut, and what should we test first?"

policy:
  allow_implicit_invocation: true
